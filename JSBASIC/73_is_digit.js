// 73	73_is_digit.js	Check whether a given character is a digit (0–9).

let ch = '7';
console.log("Character:", ch);
if (ch >= '0' && ch <= '9') {
    console.log(ch, "is a Digit.");
} else {
    console.log(ch, "is NOT a Digit.");
}
