// 151	151_array_print.js	Create an array and print all its elements using a loop.

let arr = [10, 20, 30, 40, 50];
console.log("Array:", arr);
console.log("Elements:");
for (let i = 0; i < arr.length; i++) {
    console.log("Index " + i + ": " + arr[i]);
}
