// 28	28_power_operator.js	Calculate the power of a number using the ** exponentiation operator.

let base = 2;
let exponent = 5;
let result = base ** exponent;
console.log(`${base} ** ${exponent} =`, result);
