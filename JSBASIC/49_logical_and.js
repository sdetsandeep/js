// 49	49_logical_and.js	Use logical AND (&&) to check if both conditions are true.

let age = 25;
let hasID = true;
console.log("Age:", age, "| Has ID:", hasID);
console.log("Age >= 18 && hasID:", age >= 18 && hasID);
console.log("Age >= 30 && hasID:", age >= 30 && hasID);
