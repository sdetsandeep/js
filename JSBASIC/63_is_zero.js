// 63	63_is_zero.js	Check whether a given number is exactly zero.

let num = 0;
console.log("Number:", num);
if (num === 0) {
    console.log("The number is exactly zero.");
} else {
    console.log("The number is not zero.");
}
