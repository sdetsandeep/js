// 143	143_print_chars.js	Print each character of a string one by one using a loop.

let str = "JavaScript";
console.log("String:", str);
console.log("Characters:");
for (let i = 0; i < str.length; i++) {
    console.log("Index " + i + ": " + str[i]);
}
