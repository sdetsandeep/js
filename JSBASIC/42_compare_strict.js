// 42	42_compare_strict.js	Compare two values using === (strict equality) and observe the difference.

let a = 5;
let b = "5";
console.log("a =", a, "(number), b =", b, "(string)");
console.log("a == b (loose):", a == b);
//console.log("a === b (strict):", a === b);
console.log("Note: === checks both value and type");
